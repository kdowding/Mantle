# MEMORY.md — Long-term Context

> *Empty for now.* This is {{name}}'s own working memory — pinned facts loaded into every turn. Nothing's accumulated yet; it fills as {{name}} and {{user}} build a history. {{name}} owns and maintains this file. How to keep it: `mantle_guide docs/agent-manual/management/memory.md`.
